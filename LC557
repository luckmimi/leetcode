class Solution:
    def reverseWords(self, s: str) -> str:
        if len(s) == 1: return s
        res = s.split(' ')
        for i in range(len(res)):
            res[i] = res[i][::-1]
        return ' '.join(res)
